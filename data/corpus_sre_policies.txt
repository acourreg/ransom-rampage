--- CORPUS 2 ---

#### [Bottleneck-Elimination-Sprint]

**id**: `throughput_optimization_sprint`
**category**: `performance`
**subcategory**: `optimization`
**source**: `Google SRE Book`
**applies_when**: `middleware throughput < 5`

**description**: Identifying the code paths that are currently choking our revenue and rewriting them in something faster than Python. It’s a surgical strike against the "technical debt" we've been ignoring since the Series B.

**game_effects**:
  node_targets: `middleware`
  metrics_delta:
    - metric: `throughput`
      delta: `+4`
      condition: `always`
    - metric: `cost`
      delta: `-1`
      condition: `always`
  company_delta:
    - metric: `cash`
      delta: `+20`
      condition: `always`
  duration_turns: `2`
  activation_cost_eur_k: `40`
  recurring_cost_eur_k_per_turn: `0`

**trade_offs**:
  upside: Direct increase in processing capacity and immediate revenue lift.
  downside: Diverts engineering talent away from feature development for two weeks.
  revenue_risk: `none`

**cto_pitch**: We unlock €20k/turn in extra revenue by removing the middleware clog for a one-time fee.

**tags**: `throughput, efficiency, performance`

---

#### [Elastic-Auto-Scaling]

**id**: `dynamic_capacity_scaling`
**category**: `infrastructure`
**subcategory**: `scaling`
**source**: `AWS Well-Architected`
**applies_when**: `middleware throughput < 5 or any flow current_revenue < base_revenue * 0.7`

**description**: Throwing money at the problem until the latency stops screaming. It’s essentially paying the cloud provider to hide the fact that our code isn't efficient enough for peak load.

**game_effects**:
  node_targets: `server | middleware`
  metrics_delta:
    - metric: `throughput`
      delta: `+5`
      condition: `always`
    - metric: `cost`
      delta: `+3`
      condition: `always`
  company_delta:
    - metric: `cash`
      delta: `+100`
      condition: `if_revenue_critical`
  duration_turns: `permanent`
  activation_cost_eur_k: `60`
  recurring_cost_eur_k_per_turn: `20`

**trade_offs**:
  upside: The site never goes down during a marketing surge.
  downside: Your monthly cloud bill will look like a phone number.
  revenue_risk: `low`

**cto_pitch**: We trade €20k/turn in cloud costs for €100k/turn in protected revenue during peaks.

**tags**: `scaling, cloud, availability`

---

#### [RPO-RTO-Backup-Tuning]

**id**: `disaster_recovery_optimization`
**category**: `reliability`
**subcategory**: `backup`
**source**: `DORA Metrics`
**applies_when**: `multiple nodes offline`

**description**: Tuning the knobs to ensure we lose minutes of data, not months, when things go sideways. Because "we have backups" is a lie until you've tried to restore them during an actual fire.

**game_effects**:
  node_targets: `database`
  metrics_delta:
    - metric: `defense`
      delta: `+1`
      condition: `always`
  company_delta:
    - metric: `breach_timer`
      delta: `-4`
      condition: `always`
    - metric: `reputation`
      delta: `+20`
      condition: `if_breach_active`
  duration_turns: `3`
  activation_cost_eur_k: `100`
  recurring_cost_eur_k_per_turn: `5`

**trade_offs**:
  upside: Drastically reduces downtime and data loss during a major incident.
  downside: Requires expensive, high-frequency snapshotting storage.
  revenue_risk: `none`

**cto_pitch**: Reduces downtime recovery by 4 turns. Protects brand reputation from permanent damage.

**tags**: `reliability, rpo, rto, recovery`

---

#### [Observability-Stack-Upgrade]

**id**: `prometheus_grafana_deployment`
**category**: `observability`
**subcategory**: `monitoring`
**source**: `Google SRE Book`
**applies_when**: `visibility < 3`

**description**: Moving from "is it up?" to "why is it weird?" Adding enough dashboards to make the office look like NASA, hopefully spotting failures before the customers start tweeting.

**game_effects**:
  node_targets: `all`
  metrics_delta:
    - metric: `visibility`
      delta: `+5`
      condition: `always`
  company_delta:
    - metric: `breach_timer`
      delta: `+1`
      condition: `always`
  duration_turns: `permanent`
  activation_cost_eur_k: `80`
  recurring_cost_eur_k_per_turn: `10`

**trade_offs**:
  upside: Total transparency into system health; no more "mystery" slowdowns.
  downside: Developers spend more time tweaking dashboard colors than fixing bugs.
  revenue_risk: `low`

**cto_pitch**: High visibility cuts through the fog. Improves mean-time-to-detection by 50%.

**tags**: `observability, monitoring, dashboards`

---

#### [Centralized-Logging-Aggregator]

**id**: `loki_elk_centralization`
**category**: `observability`
**subcategory**: `logging`
**source**: `Google SRE Book`
**applies_when**: `database node fogged`

**description**: Searching for an error in distributed systems without central logs is like looking for a needle in a burning haystack. This puts the needles in one searchable place.

**game_effects**:
  node_targets: `all`
  metrics_delta:
    - metric: `visibility`
      delta: `+3`
      condition: `always`
    - metric: `fogged`
      delta: `set:false`
      condition: `always`
  company_delta:
    - metric: `cash`
      delta: `-5`
      condition: `always`
  duration_turns: `permanent`
  activation_cost_eur_k: `90`
  recurring_cost_eur_k_per_turn: `15`

**trade_offs**:
  upside: Makes post-mortem analysis actually possible rather than speculative.
  downside: The cloud storage providers will name their next yacht after our log bill.
  revenue_risk: `none`

**cto_pitch**: Eliminates "fog of war" on our nodes for a flat €15k/turn fee.

**tags**: `logging, observability, troubleshooting`

---

#### [Database-Read-Replicas]

**id**: `db_sharding_replicas`
**category**: `infrastructure`
**subcategory**: `database`
**source**: `AWS Well-Architected`
**applies_when**: `database node fogged or node cost > 7`

**description**: Cloning the data so the reports don't stop the customers from buying things. Stopping the "one big database" from becoming a single point of failure and a massive bottleneck.

**game_effects**:
  node_targets: `database`
  metrics_delta:
    - metric: `throughput`
      delta: `+4`
      condition: `always`
    - metric: `defense`
      delta: `-1`
      condition: `always`
  company_delta:
    - metric: `cash`
      delta: `+40`
      condition: `always`
  duration_turns: `5`
  activation_cost_eur_k: `150`
  recurring_cost_eur_k_per_turn: `25`

**trade_offs**:
  upside: Massive boost to customer-facing performance and revenue.
  downside: "Eventual consistency" means some customers might see slightly outdated data.
  revenue_risk: `moderate`

**cto_pitch**: Increases transaction throughput by 40%, yielding €40k/turn in growth.

**tags**: `database, throughput, scale`

---

#### [Async-Architecture-Pivot]

**id**: `event_driven_migration`
**category**: `architecture`
**subcategory**: `event_driven`
**source**: `Google SRE Book`
**applies_when**: `middleware throughput < 3`

**description**: Stop making the user wait for a synchronous response. Moving to an architecture where things happen "eventually," which is much faster but twice as hard to explain to the CEO.

**game_effects**:
  node_targets: `middleware`
  metrics_delta:
    - metric: `throughput`
      delta: `+6`
      condition: `always`
    - metric: `visibility`
      delta: `-2`
      condition: `always`
  company_delta:
    - metric: `cash`
      delta: `+60`
      condition: `always`
  duration_turns: `permanent`
  activation_cost_eur_k: `250`
  recurring_cost_eur_k_per_turn: `5`

**trade_offs**:
  upside: Huge throughput gains; the system becomes much harder to crash with load.
  downside: Debugging becomes a nightmare of "where did that message go?"
  revenue_risk: `high`

**cto_pitch**: We double our capacity for €250k. It’s the only way to scale for 2026 volumes.

**tags**: `async, event_driven, throughput`

---

#### [Multi-Cloud-Diversification]

**id**: `vendor_agnostic_cloud`
**category**: `infrastructure`
**subcategory**: `cloud`
**source**: `DORA Metrics`
**applies_when**: `vendor node visibility < 4`

**description**: Spreading our eggs across multiple baskets so that a single region outage doesn't take the company offline. Because even the "Big Three" cloud providers have bad days.

**game_effects**:
  node_targets: `vendor | server`
  metrics_delta:
    - metric: `defense`
      delta: `+2`
      condition: `always`
  company_delta:
    - metric: `reputation`
      delta: `+30`
      condition: `if_breach_active`
    - metric: `cash`
      delta: `-40`
      condition: `always`
  duration_turns: `permanent`
  activation_cost_eur_k: `400`
  recurring_cost_eur_k_per_turn: `50`

**trade_offs**:
  upside: Near-perfect availability during major cloud provider outages.
  downside: Complexity is doubled, and we lose all our "bulk buy" discounts.
  revenue_risk: `low`

**cto_pitch**: Eliminates 3rd-party vendor risk. It's "anti-bankruptcy" insurance for the cloud.

**tags**: `multi_cloud, availability, redundancy`

---

#### [SLA-Enforcement-Shield]

**id**: `service_level_monitoring`
**category**: `governance`
**subcategory**: `sla`
**source**: `Google SRE Book`
**applies_when**: `any flow current_revenue < base_revenue * 0.5`

**description**: Turning "it's slow" into a legal document. Defining exactly what constitutes "broken" and holding teams (and vendors) accountable for it.

**game_effects**:
  node_targets: `all`
  metrics_delta:
    - metric: `throughput`
      delta: `+2`
      condition: `always`
  company_delta:
    - metric: `compliance`
      delta: `+0.2`
      condition: `always`
    - metric: `cash`
      delta: `+15`
      condition: `if_compliance_lt_0.5`
  duration_turns: `permanent`
  activation_cost_eur_k: `50`
  recurring_cost_eur_k_per_turn: `5`

**trade_offs**:
  upside: Drives a culture of reliability and allows us to claw back money from failing vendors.
  downside: We might find out we're the ones failing our own SLAs.
  revenue_risk: `none`

**cto_pitch**: Standardizes performance. Increases net income by €15k/turn via vendor penalties.

**tags**: `sla, slo, reliability`

---

#### [FinOps-Cloud-Audit]

**id**: `cost_optimization_drive`
**category**: `finops`
**subcategory**: `cost`
**source**: `FinOps Foundation`
**applies_when**: `node cost > 7 or net_income negative for 2+ turns`

**description**: A ruthless hunt for the "Zombie" servers and over-provisioned databases eating our budget. It’s like finding €20 bills in the laundry, if your laundry was a data center.

**game_effects**:
  node_targets: `all`
  metrics_delta:
    - metric: `cost`
      delta: `-3`
      condition: `always`
  company_delta:
    - metric: `cash`
      delta: `+80`
      condition: `always`
  duration_turns: `2`
  activation_cost_eur_k: `30`
  recurring_cost_eur_k_per_turn: `0`

**trade_offs**:
  upside: Immediate boost to the bottom line without cutting staff.
  downside: We might accidentally delete the "critical" test environment for a major project.
  revenue_risk: `moderate`

**cto_pitch**: One-time €30k spend to recover €80k/turn in wasted cloud spend.

**tags**: `finops, cost_reduction, cloud`

---

#### [Chaos-Engineering-Drills]

**id**: `fault_injection_testing`
**category**: `reliability`
**subcategory**: `chaos`
**source**: `Principles of Chaos`
**applies_when**: `defense > 5`

**description**: Breaking things on purpose during office hours so they don't break on their own at 2 AM. Like a fire drill, but we actually start a small, controlled fire in the server room.

**game_effects**:
  node_targets: `server | middleware`
  metrics_delta:
    - metric: `defense`
      delta: `+3`
      condition: `always`
    - metric: `visibility`
      delta: `+2`
      condition: `always`
  company_delta:
    - metric: `breach_timer`
      delta: `-2`
      condition: `always`
  duration_turns: `3`
  activation_cost_eur_k: `110`
  recurring_cost_eur_k_per_turn: `5`

**trade_offs**:
  upside: Forces the system to become resilient; outages become non-events.
  downside: "Controlled" can become "uncontrolled" very quickly if you're not careful.
  revenue_risk: `high`

**cto_pitch**: We test our resilience before the attackers do. Improves survival by 2 turns.

**tags**: `chaos, resilience, reliability`

---

#### [Feature-Flag-Rollout]

**id**: `progressive_delivery_toggles`
**category**: `delivery`
**subcategory**: `deployment`
**source**: `DORA Metrics`
**applies_when**: `any flow current_revenue < base_revenue * 0.9`

**description**: Giving ourselves a "Kill Switch" for every new feature. If the update breaks the checkout page, we just flip a switch instead of waiting for a 4-hour rollback.

**game_effects**:
  node_targets: `entry`
  metrics_delta:
    - metric: `throughput`
      delta: `+2`
      condition: `always`
  company_delta:
    - metric: `reputation`
      delta: `+10`
      condition: `always`
    - metric: `cash`
      delta: `+20`
      condition: `if_revenue_critical`
  duration_turns: `permanent`
  activation_cost_eur_k: `130`
  recurring_cost_eur_k_per_turn: `10`

**trade_offs**:
  upside: Faster recovery from bad updates. Decouples "deploying" from "releasing."
  downside: Adds significant technical debt and conditional logic mess to the codebase.
  revenue_risk: `low`

**cto_pitch**: Protects revenue during new launches. Cuts down on "failed release" PR hits.

**tags**: `feature_flags, delivery, dora`

---

#### [API-Gateway-Rate-Limiter]

**id**: `api_rate_limiting`
**category**: `infrastructure`
**subcategory**: `traffic_management`
**source**: `Google SRE Book`
**applies_when**: `middleware throughput < 4`

**description**: Putting a speed limit on our digital roads. Stopping that one "enthusiastic" partner from crawling our entire site 100 times a second and crashing the database.

**game_effects**:
  node_targets: `entry`
  metrics_delta:
    - metric: `throughput`
      delta: `+3`
      condition: `always`
    - metric: `defense`
      delta: `+2`
      condition: `always`
  company_delta:
    - metric: `cash`
      delta: `+10`
      condition: `always`
  duration_turns: `permanent`
  activation_cost_eur_k: `70`
  recurring_cost_eur_k_per_turn: `2`

**trade_offs**:
  upside: Stabilizes system performance and prevents "Death by a Thousand Scrapers."
  downside: Legitimate high-volume partners will hit the limit and start calling your phone.
  revenue_risk: `low`

**cto_pitch**: Stabilizes throughput and revenue for a tiny €2k/turn maintenance cost.

**tags**: `api, traffic, stabilization`

---

#### [Circuit-Breaker-Pattern]

**id**: `circuit_breaker_implementation`
**category**: `architecture`
**subcategory**: `resilience`
**source**: `Release It! (Nygard)`
**applies_when**: `multiple nodes offline`

**description**: If a downstream service is failing, stop calling it. Don't let a slow database drag the entire customer experience into the abyss. Fail fast, recover faster.

**game_effects**:
  node_targets: `middleware | server`
  metrics_delta:
    - metric: `throughput`
      delta: `+2`
      condition: `always`
    - metric: `defense`
      delta: `+1`
      condition: `always`
  company_delta:
    - metric: `breach_timer`
      delta: `-1`
      condition: `always`
  duration_turns: `permanent`
  activation_cost_eur_k: `120`
  recurring_cost_eur_k_per_turn: `0`

**trade_offs**:
  upside: Prevents cascading failures that turn minor glitches into total outages.
  downside: Users see "Service Unavailable" instead of "Loading..."—which is technically an outage too.
  revenue_risk: `moderate`

**cto_pitch**: Prevents system-wide meltdowns. One-time cost to ensure we never cascade.

**tags**: `resilience, patterns, cascading_failure`

---

#### [Service-Mesh-Intelligence]

**id**: `service_mesh_istio`
**category**: `infrastructure`
**subcategory**: `networking`
**source**: `CNCF`
**applies_when**: `visibility < 4 or middleware throughput < 5`

**description**: Adding a tiny sidecar to every service to handle the "plumbing." Like upgrading from walkie-talkies to a fully encrypted, monitored phone network for all internal traffic.

**game_effects**:
  node_targets: `middleware | server`
  metrics_delta:
    - metric: `visibility`
      delta: `+4`
      condition: `always`
    - metric: `defense`
      delta: `+2`
      condition: `always`
  company_delta:
    - metric: `compliance`
      delta: `+0.1`
      condition: `always`
  duration_turns: `5`
  activation_cost_eur_k: `220`
  recurring_cost_eur_k_per_turn: `35`

**trade_offs**:
  upside: Out-of-the-box encryption and deep observability for all internal traffic.
  downside: CPU overhead will require us to buy more servers just to run the "plumbing."
  revenue_risk: `low`

**cto_pitch**: The ultimate visibility and security upgrade for a complex microservices estate.

**tags**: `service_mesh, kubernetes, observability`

---

#### [GitOps-IaC-Enforcement]

**id**: `infrastructure_as_code_pipeline`
**category**: `delivery`
**subcategory**: `gitops`
**source**: `DORA Metrics`
**applies_when**: `compliance < 0.6`

**description**: No more "cowboy" changes in the production console. If it’s not in the code, it doesn't exist. Forces an audit trail on every single server change.

**game_effects**:
  node_targets: `server`
  metrics_delta:
    - metric: `compliance_score`
      delta: `+0.4`
      condition: `always`
    - metric: `defense`
      delta: `+2`
      condition: `always`
  company_delta:
    - metric: `compliance`
      delta: `set:0.9`
      condition: `always`
  duration_turns: `permanent`
  activation_cost_eur_k: `160`
  recurring_cost_eur_k_per_turn: `10`

**trade_offs**:
  upside: Recovery becomes as simple as "re-running the script." No more snowflakes.
  downside: A single typo in a script can now delete the entire production environment in seconds.
  revenue_risk: `high`

**cto_pitch**: Guarantees compliance and makes disaster recovery a 5-minute task.

**tags**: `gitops, iac, compliance, automation`

---

#### [SLO-On-Call-Rotation]

**id**: `sre_oncall_definition`
**category**: `reliability`
**subcategory**: `governance`
**source**: `Google SRE Book`
**applies_when**: `multiple nodes offline`

**description**: Actually paying people to care about the systems on the weekend. Ensuring they have the power to say "no" to new features if the system is unstable.

**game_effects**:
  node_targets: `all`
  metrics_delta:
    - metric: `visibility`
      delta: `+2`
      condition: `always`
  company_delta:
    - metric: `breach_timer`
      delta: `-3`
      condition: `if_breach_active`
    - metric: `reputation`
      delta: `+5`
      condition: `always`
  duration_turns: `permanent`
  activation_cost_eur_k: `80`
  recurring_cost_eur_k_per_turn: `25`

**trade_offs**:
  upside: Faster response times and a workforce that isn't burnt out from "hidden" work.
  downside: It turns out that people want to be paid real money for interrupted sleep.
  revenue_risk: `none`

**cto_pitch**: Faster incident recovery. €25k/turn buys us a 3-turn faster breach resolution.

**tags**: `sre, slo, reliability, culture`

---

#### [DB-Connection-Pooling]

**id**: `database_connection_optimization`
**category**: `infrastructure`
**subcategory**: `database`
**source**: `Google SRE Book`
**applies_when**: `database node fogged and throughput < 5`

**description**: Reusing database connections instead of opening a new one for every request. Like carpooling instead of everyone taking their own bus to work.

**game_effects**:
  node_targets: `database`
  metrics_delta:
    - metric: `throughput`
      delta: `+3`
      condition: `always`
    - metric: `cost`
      delta: `-2`
      condition: `always`
  company_delta:
    - metric: `cash`
      delta: `+10`
      condition: `always`
  duration_turns: `2`
  activation_cost_eur_k: `40`
  recurring_cost_eur_k_per_turn: `0`

**trade_offs**:
  upside: Squeezes more life out of our current (expensive) database servers.
  downside: Can mask underlying architectural issues that will bite us later.
  revenue_risk: `low`

**cto_pitch**: Immediate throughput gain and €10k/turn savings for a one-time sprint.

**tags**: `database, throughput, cost_reduction`

---

#### [Edge-Content-Caching]

**id**: `cdn_edge_acceleration`
**category**: `performance`
**subcategory**: `cdn`
**source**: `DORA Metrics`
**applies_when**: `any flow current_revenue < base_revenue * 0.8`

**description**: Moving our data closer to the customer so it doesn't have to travel across the ocean for every click. Speed of light working in our favor.

**game_effects**:
  node_targets: `entry`
  metrics_delta:
    - metric: `throughput`
      delta: `+5`
      condition: `always`
    - metric: `cost`
      delta: `+2`
      condition: `always`
  company_delta:
    - metric: `cash`
      delta: `+45`
      condition: `always`
  duration_turns: `permanent`
  activation_cost_eur_k: `90`
  recurring_cost_eur_k_per_turn: `15`

**trade_offs**:
  upside: Massive improvement in UX and a 50% reduction in origin server load.
  downside: "Cache invalidation" is still the hardest problem in computer science.
  revenue_risk: `moderate`

**cto_pitch**: Boosts revenue by €45k/turn by making the site feel "instant" worldwide.

**tags**: `cdn, throughput, performance`

---

#### [Incident-Learning-Culture]

**id**: `post_mortem_blameless`
**category**: `governance`
**subcategory**: `culture`
**source**: `Google SRE Book`
**applies_when**: `breach_timer > 0 or multiple nodes offline`

**description**: Learning why we failed without pointing fingers. Turning our mistakes into an expensive educational program so we don't repeat them.

**game_effects**:
  node_targets: `all`
  metrics_delta:
    - metric: `defense`
      delta: `+2`
      condition: `always`
    - metric: `visibility`
      delta: `+1`
      condition: `always`
  company_delta:
    - metric: `breach_timer`
      delta: `-2`
      condition: `if_breach_active`
    - metric: `reputation`
      delta: `+15`
      condition: `always`
  duration_turns: `3`
  activation_cost_eur_k: `20`
  recurring_cost_eur_k_per_turn: `0`

**trade_offs**:
  upside: Prevents the same mistake from happening twice. Boosts morale.
  downside: Takes a lot of time and requires engineers to be brutally honest.
  revenue_risk: `none`

**cto_pitch**: Minimal cost for a long-term defense boost and reputation recovery.

**tags**: `post_mortem, culture, reliability`

---

### ADDITIONAL RESEARCH-BACKED POLICIES (2026 TRENDS)

#### [Carbon-Aware-Workload-Shifting]

**id**: `greenops_sustainability_scaling`
**category**: `finops`
**subcategory**: `sustainability`
**source**: `Green Software Foundation / ISO 14001`
**applies_when**: `node cost > 8 and net_income negative`

**description**: Shifting non-critical workloads to regions with greener grids or lower power costs. It’s PR-friendly and actually saves us money on our carbon-taxed cloud bill.

**game_effects**:
  node_targets: `server`
  metrics_delta:
    - metric: `cost`
      delta: `-3`
      condition: `always`
    - metric: `throughput`
      delta: `-1`
      condition: `always`
  company_delta:
    - metric: `reputation`
      delta: `+20`
      condition: `always`
  duration_turns: `permanent`
  activation_cost_eur_k: `70`
  recurring_cost_eur_k_per_turn: `0`

**trade_offs**:
  upside: Boosts ESG scores and reduces operational costs.
  downside: Slightly higher latency as your batch jobs are now running in Iceland.
  revenue_risk: `low`

**cto_pitch**: Cuts costs by €30k/turn while boosting our market reputation by 20 points.

**tags**: `sustainability, greenops, cost_reduction`

---

#### [eBPF-Deep-Observability]

**id**: `kernel_level_monitoring`
**category**: `observability`
**subcategory**: `telemetry`
**source**: `Cilium / CNCF`
**applies_when**: `visibility < 4 and Byte AP >= 2`

**description**: Watching the system from the kernel level. No sidecars, no overhead, just pure visibility into every packet and syscall. It's like having X-ray vision for the data center.

**game_effects**:
  node_targets: `server | middleware`
  metrics_delta:
    - metric: `visibility`
      delta: `+5`
      condition: `always`
    - metric: `throughput`
      delta: `+1`
      condition: `always`
  company_delta:
    - metric: `breach_timer`
      delta: `-1`
      condition: `if_breach_active`
  duration_turns: `permanent`
  activation_cost_eur_k: `180`
  recurring_cost_eur_k_per_turn: `25`

**trade_offs**:
  upside: Unmatched visibility with almost zero performance penalty.
  downside: Requires a modern kernel and engineers who actually understand Linux.
  revenue_risk: `none`

**cto_pitch**: Maximum visibility for €25k/turn. Spot a breach before it even finishes a syscall.

**tags**: `ebpf, observability, performance`

---

#### [Internal-Developer-Portal]

**id**: `backstage_idp_deployment`
**category**: `platform_engineering`
**subcategory**: `self_service`
**source**: `Platform Engineering Handbook`
**applies_when**: `net_income negative and middleware throughput < 6`

**description**: A vending machine for infrastructure. Developers can spin up what they need without filing a ticket and waiting for three weeks. Removes "Ticket-Ops" from our vocabulary.

**game_effects**:
  node_targets: `all`
  metrics_delta:
    - metric: `cost`
      delta: `-2`
      condition: `always`
    - metric: `throughput`
      delta: `+3`
      condition: `always`
  company_delta:
    - metric: `cash`
      delta: `+50`
      condition: `always`
  duration_turns: `permanent`
  activation_cost_eur_k: `150`
  recurring_cost_eur_k_per_turn: `10`

**trade_offs**:
  upside: Massive increase in developer velocity and reduction in management overhead.
  downside: If the "vending machine" is configured wrong, they can spin up a €10k server in one click.
  revenue_risk: `moderate`

**cto_pitch**: Replaces manual labor with automation. Increases throughput and saves €50k/turn.

**tags**: `idp, platform_engineering, velocity`

---

#### [AIOps-Auto-Remediation]

**id**: `automated_incident_response`
**category**: `reliability`
**subcategory**: `aiops`
**source**: `Google SRE (AIOps section)`
**applies_when**: `multiple nodes offline and breach_timer == 0`

**description**: Letting an AI handle the "turn it off and on again" phase of an incident. It learns from our previous post-mortems and fixes common issues before the on-call engineer even finds their glasses.

**game_effects**:
  node_targets: `all`
  metrics_delta:
    - metric: `visibility`
      delta: `+2`
      condition: `always`
  company_delta:
    - metric: `breach_timer`
      delta: `-3`
      condition: `always`
    - metric: `cash`
      delta: `-20`
      condition: `always`
  duration_turns: `4`
  activation_cost_eur_k: `200`
  recurring_cost_eur_k_per_turn: `40`

**trade_offs**:
  upside: Drastically reduces Mean Time to Repair (MTTR).
  downside: The AI might "fix" things by deleting the database if it thinks that's the fastest way to stop an error.
  revenue_risk: `high`

**cto_pitch**: Cuts recovery time by 3 turns. High price, but it works while you sleep.

**tags**: `aiops, automation, mttr`

---

#### [Network-Egress-Optimization]

**id**: `topology_aware_routing`
**category**: `finops`
**subcategory**: `networking`
**source**: `AWS Well-Architected / FinOps`
**applies_when**: `node cost > 7`

**description**: Keeping data traffic within the same availability zone. Cloud providers charge us for every byte that crosses a "border"—this policy forces the data to take the local road.

**game_effects**:
  node_targets: `middleware | server`
  metrics_delta:
    - metric: `cost`
      delta: `-4`
      condition: `always`
    - metric: `throughput`
      delta: `+1`
      condition: `always`
  company_delta:
    - metric: `cash`
      delta: `+60`
      condition: `always`
  duration_turns: `permanent`
  activation_cost_eur_k: `50`
  recurring_cost_eur_k_per_turn: `5`

**trade_offs**:
  upside: Substantial reduction in cloud networking costs.
  downside: Reduces the system's ability to automatically failover to another zone.
  revenue_risk: `moderate`

**cto_pitch**: Saves €60k/turn in hidden network fees for a minor architectural tweak.

**tags**: `networking, finops, egress_costs`

---

#### [Dynamic-SLO-Budgeting]

**id**: `business_aligned_reliability`
**category**: `reliability`
**subcategory**: `slo`
**source**: `Google SRE Book`
**applies_when**: `any flow current_revenue < base_revenue * 0.5`

**description**: Automatically tightening our error budgets during high-revenue events (like Black Friday) and loosening them when things are quiet. Reliability that actually follows the money.

**game_effects**:
  node_targets: `all`
  metrics_delta:
    - metric: `throughput`
      delta: `+2`
      condition: `if_revenue_critical`
  company_delta:
    - metric: `cash`
      delta: `+100`
      condition: `if_revenue_critical`
    - metric: `reputation`
      delta: `+10`
      condition: `always`
  duration_turns: `3`
  activation_cost_eur_k: `40`
  recurring_cost_eur_k_per_turn: `10`

**trade_offs**:
  upside: Maximizes revenue during peak periods by prioritizing stability over new features.
  downside: Product managers will be annoyed when their "critical" update is blocked for two weeks.
  revenue_risk: `low`

**cto_pitch**: Protects €100k/turn of peak revenue by enforcing strict stability when it matters.

**tags**: `slo, reliability, business_value`

---

#### [Serverless-Warmup-Strategy]

**id**: `cold_start_mitigation`
**category**: `performance`
**subcategory**: `serverless`
**source**: `AWS Lambda Best Practices`
**applies_when**: `middleware throughput < 4`

**description**: Keeping our "on-demand" functions awake so they don't take 5 seconds to start when a customer arrives. It’s essentially paying a "stay awake" tax to the cloud provider.

**game_effects**:
  node_targets: `middleware`
  metrics_delta:
    - metric: `throughput`
      delta: `+4`
      condition: `always`
    - metric: `cost`
      delta: `+2`
      condition: `always`
  company_delta:
    - metric: `cash`
      delta: `+30`
      condition: `always`
  duration_turns: `permanent`
  activation_cost_eur_k: `30`
  recurring_cost_eur_k_per_turn: `12`

**trade_offs**:
  upside: Eliminates the "cold start" latency that makes our app feel sluggish.
  downside: You're paying for compute time even when no one is using the app.
  revenue_risk: `none`

**cto_pitch**: Improves system response time by 40% for a predictable €12k/turn fee.

**tags**: `serverless, latency, performance`

---

#### [IaC-Drift-Detection]

**id**: `infrastructure_drift_guardrails`
**category**: `infrastructure`
**subcategory**: `gitops`
**source**: `DORA Metrics`
**applies_when**: `compliance < 0.5 and node cost > 6`

**description**: A watchdog that barks whenever someone manually changes a setting in the cloud console. It ensures our "Code" matches our "Reality," preventing security holes and cost spikes.

**game_effects**:
  node_targets: `server | database`
  metrics_delta:
    - metric: `defense`
      delta: `+3`
      condition: `always`
    - metric: `compliance_score`
      delta: `+0.3`
      condition: `always`
  company_delta:
    - metric: `compliance`
      delta: `+0.2`
      condition: `always`
  duration_turns: `permanent`
  activation_cost_eur_k: `60`
  recurring_cost_eur_k_per_turn: `5`

**trade_offs**:
  upside: Stops "Shadow IT" and manual errors from compromising our security posture.
  downside: Developers will find it "annoying" when their quick fixes are automatically reverted.
  revenue_risk: `low`

**cto_pitch**: Guarantees our infrastructure stays exactly as we designed it. Prevents compliance decay.

**tags**: `iac, drift, compliance, security`

---

#### [Secret-Zero-Elimination]

**id**: `workload_identity_spiffe`
**category**: `infrastructure`
**subcategory**: `iam`
**source**: `SPIFFE / SPIRE`
**applies_when**: `database node visible and unprotected`

**description**: Removing static passwords from our code and replacing them with short-lived, cryptographic identities. If an attacker steals a "credential," it will already be useless.

**game_effects**:
  node_targets: `middleware | database`
  metrics_delta:
    - metric: `defense`
      delta: `+4`
      condition: `always`
  company_delta:
    - metric: `breach_timer`
      delta: `+2`
      condition: `if_breach_active`
  duration_turns: `permanent`
  activation_cost_eur_k: `180`
  recurring_cost_eur_k_per_turn: `15`

**trade_offs**:
  upside: Makes "credential theft" nearly impossible for an attacker to exploit.
  downside: Significant architectural shift; everything needs a certificate now.
  revenue_risk: `moderate`

**cto_pitch**: Deletes the risk of "leaked passwords" from our databases for good.

**tags**: `spiffe, zero_trust, iam, identity`

---

#### [Mobile-Edge-Deployment]

**id**: `mec_latency_reduction`
**category**: `performance`
**subcategory**: `edge`
**source**: `5G MEC Standards`
**applies_when**: `any flow current_revenue < base_revenue * 0.7`

**description**: Moving our logic into the mobile carrier's network. We're cutting out the entire public internet for our mobile users, making the app feel like it's running locally on their device.

**game_effects**:
  node_targets: `entry | middleware`
  metrics_delta:
    - metric: `throughput`
      delta: `+6`
      condition: `always`
  company_delta:
    - metric: `cash`
      delta: `+120`
      condition: `always`
    - metric: `reputation`
      delta: `+10`
      condition: `always`
  duration_turns: `5`
  activation_cost_eur_k: `300`
  recurring_cost_eur_k_per_turn: `60`

**trade_offs**:
  upside: Unbeatable speed for the mobile-first customer base.
  downside: Obscenely expensive and ties us to specific carrier infrastructure.
  revenue_risk: `high`

**cto_pitch**: The ultimate performance move. Adds €120k/turn to the top line via superior UX.

**tags**: `edge, performance, mobile, revenue_growth`